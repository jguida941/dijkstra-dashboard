[pytest]
qt_api = pyqt6
testpaths = tests
addopts = --strict-markers -v
markers =
    slow: marks tests as slow (large graphs)
    ui: marks tests requiring Qt
    integration: marks integration tests
